name: bigdata
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy<2
  - pandas>=2.0
  - yfinance>=0.2
  - pytest>=7.0
  - pytest-cov>=4.0
  - pip:
      - -e ".[test]"
